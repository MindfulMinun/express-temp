{
  "name": "express-temp",
  "version": "0.1.0",
  "description": "A template for Node.js Express apps.",
  "main": "dist/server.js",
  "author": "MindfulMinun <importantmails4bc@gmail.com> (https://benjic.xyz)",
  "contributors": [],
  "homepage": "https://github.com/MindfulMinun/express-temp",
  "repository": "github:MindfulMinun/express-temp",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && babel src -d dist",
    "serve": "node dist/server.js",
    "watch": "nodemon --watch src --exec 'npm run build && npm run serve'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=8.0.0 <11.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "dotenv": "^8.1.0",
    "nodemon": "^1.18.9"
  },
  "dependencies": {
    "express": "^4.17.3",
    "mustache-express": "^1.3.0"
  }
}
